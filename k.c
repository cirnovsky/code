#include <stdio.h>

#define N	500

int main() {
	freopen("in", "r", stdin);
	int t;

	scanf("%d", &t);
	while (t--) {
		int n;
		int i, j;
		int yes = 1;
		static char cc[N + 1];

		scanf("%d%s", &n, cc);
	}
}
